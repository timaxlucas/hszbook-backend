language: node_js
node_js:
    - "10"
cache:
  directories:
    - node_modules
services:
  - postgresql
# allow headful tests
before_script:
  - psql -U postgres -d postgres -a -f ./pg-setup.sql
  - export PGPASSWORD="replaceme"
  #- json='{"secret": "<secret>","connectionString": "postgres://postgres:replaceme@localhost:5432/hszbook"}'
  #- echo $json > config.json
